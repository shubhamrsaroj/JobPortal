<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Creative Job Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #f0f4f8;
            font-family: 'Poppins', sans-serif;
            color: #2d3748;
        }

        .container {
            max-width: 700px;
            padding: 20px;
        }

        .job-card {
            background-color: #ffffff;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            padding: 25px;
            position: relative;
            overflow: hidden;
        }

        .job-card::before {
            content: "";
            position: absolute;
            top: -50px;
            left: -50px;
            width: 100px;
            height: 100px;
            background-color: #4299e1;
            border-radius: 50%;
            opacity: 0.1;
        }

        .job-card::after {
            content: "";
            position: absolute;
            bottom: -30px;
            right: -30px;
            width: 80px;
            height: 80px;
            background-color: #48bb78;
            border-radius: 50%;
            opacity: 0.1;
        }

        .company-logo {
            width: 70px;
            height: 70px;
            background-color: #ebf4ff;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 15px;
            font-size: 1.8rem;
            font-weight: bold;
            color: #4299e1;
            box-shadow: 0 4px 6px rgba(66, 153, 225, 0.1);
        }

        .job-title {
            font-size: 1.6rem;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 5px;
        }

        .company-name {
            font-size: 1.1rem;
            color: #4a5568;
            margin-bottom: 20px;
        }

        .details-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }

        .detail-item {
            display: flex;
            align-items: center;
            background-color: #f7fafc;
            padding: 10px;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .detail-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .detail-icon {
            width: 24px;
            height: 24px;
            margin-right: 10px;
            color: #4299e1;
        }

        .detail-text {
            font-size: 0.9rem;
            color: #4a5568;
            text-decoration: none;
        }

        .skills-section {
            margin-top: 20px;
        }

        .skills-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 10px;
            color: #2d3748;
        }

        .skills-container {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
        }

        .skill-tag {
            background-color: #e6fffa;
            color: #319795;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            transition: all 0.3s ease;
        }

        .skill-tag:hover {
            background-color: #319795;
            color: #e6fffa;
        }

        .apply-button {
            background-color: #4299e1;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-block;
            text-decoration: none;
            margin-top: 20px;
        }

        .apply-button:hover {
            background-color: #3182ce;
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(66, 153, 225, 0.2);
        }

        .action-icons {
            position: absolute;
            top: 15px;
            right: 15px;
            display: flex;
            gap: 10px;
        }

        .action-icon {
            font-size: 1rem;
            color: #a0aec0;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .action-icon:hover {
            color: #4299e1;
            transform: scale(1.1);
        }

        .delete-form {
            display: inline;
        }

        .delete-button {
            background: none;
            border: none;
            color: #a0aec0;
            cursor: pointer;
            font-size: 1rem;
            padding: 0;
            transition: all 0.3s ease;
        }

        .delete-button:hover {
            color: #4299e1;
            transform: scale(1.1);
        }

        .alert {
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .alert-heading {
            font-weight: 600;
        }

        .alert-link {
            font-weight: 600;
            text-decoration: none;
            color: #4299e1;
            transition: color 0.3s ease;
        }

        .alert-link:hover {
            color: #3182ce;
        }

        #myjobapp{
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <% if (typeof job !== 'undefined' && job) { %>
            <div class="job-card">
                <% if (locals.recruiter) { %>
                    <div class="action-icons">
                        <a href="/updatenewapplication/<%= job.id %>" class="fas fa-edit action-icon" title="Edit Job"></a>
                        <form action="/deleteJob/<%= job.id %>" method="POST" class="delete-form" onsubmit="return confirm('Are you sure you want to delete this job?');">
                            <button type="submit" class="fas fa-trash delete-button" title="Delete Job"></button>
                        </form>
                    </div>
                <% } %>

                <div class="company-logo">
                    <%= job.companyName.charAt(0).toUpperCase() %>
                </div>
                <h1 class="job-title"><%= job.jobDesignation %></h1>
                <h2 class="company-name"><%= job.companyName %></h2>
                
                <div class="details-section">
                    <div class="detail-item">
                        <svg class="detail-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                            <circle cx="12" cy="10" r="3"></circle>
                        </svg>
                        <span class="detail-text"><%= job.jobLocation %></span>
                    </div>
                    <div class="detail-item">
                        <svg class="detail-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <rect x="2" y="7" width="20" height="14" rx="2" ry="2"></rect>
                            <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"></path>
                        </svg>
                        <span class="detail-text"><%= job.jobCategory %></span>
                    </div>
                    <div class="detail-item">
                        <svg class="detail-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
                        </svg>
                        <span class="detail-text"><%= job.salary %></span>
                    </div>
                    <div class="detail-item">
                        <svg class="detail-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                            <circle cx="9" cy="7" r="4"></circle>
                            <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                            <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
                        </svg>
                        <span class="detail-text">Positions: <%= job.totalPositions %> (<%= job.totalOpenings %> openings)</span>
                    </div>
                    <div class="detail-item">
                        <svg class="detail-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                            <line x1="16" y1="2" x2="16" y2="6"></line>
                            <line x1="8" y1="2" x2="8" y2="6"></line>
                            <line x1="3" y1="10" x2="21" y2="10"></line>
                        </svg>
                        <span class="detail-text">Apply By: <%= job.applyBy %></span>
                    </div>


                    <div class="detail-item">
                        <svg class="detail-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                            <circle cx="9" cy="7" r="4"></circle>
                            <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                            <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
                        </svg>
                        <span class="detail-text"><a id="myjobapp" href='/myjobseeker'> Applicants :<%= myalljob %></a></span>
                    </div>


                    


                </div>

                <div class="skills-section">
                    <h3 class="skills-title">Required Skills</h3>
                    <div class="skills-container">
                        <% job.skills.forEach(skill => { %>
                            <span class="skill-tag"><%= skill %></span>
                        <% }); %>
                    </div>
                </div>

                <a href="/applynow" class="apply-button">Apply Now</a>
            </div>
        <% } else { %>
            <div class="alert alert-warning" role="alert">
                <h4 class="alert-heading">Job Not Found</h4>
                <p>Sorry, the job you're looking for doesn't exist or has been removed.</p>
                <hr>
                <p class="mb-0">
                    <a href="#" class="alert-link">View all available jobs</a>
                </p>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>