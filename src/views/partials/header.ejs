<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Job Portal</title>

    <!-- Bootstrap 5.2.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes scaleUp {
            0% { transform: scale(0.8); opacity: 0; }
            100% { transform: scale(1); opacity: 1; }
        }

        body {
    font-family: 'Poppins', sans-serif;
    background: url("https://media.istockphoto.com/id/1394701218/photo/job-search-concept-find-your-career-woman-looking-at-online-website-by-laptop-computer-people.jpg?s=2048x2048&w=is&k=20&c=sGBbWd2DfQYo-p_ObXZOUWV-Vl91i9u1YWfQ-jiE3KM=");
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
    min-height: 100vh;
}

.overlay {
    display: none; /* Remove the overlay effect */
}

.content-wrapper {
    background-color: rgba(255, 255, 255, 0.9);
    border-radius: 15px;
    box-shadow: 0 10px 30px rgba(0,0,0,.2);
    overflow: hidden;
    margin-top: 5rem;
    animation: fadeInUp 1s ease-out;
}

.content {
    padding: 3rem;
}

h1 {
    color: #3498db;
    font-weight: 700;
    margin-bottom: 1rem;
    animation: scaleUp 1s ease-out 0.2s;
    opacity: 0;
    animation-fill-mode: forwards;
}

h2 {
    color: #2ecc71;
    font-weight: 600;
    margin-bottom: 2rem;
    animation: scaleUp 1s ease-out 0.4s;
    opacity: 0;
    animation-fill-mode: forwards;
}

.subtitle {
    font-size: 1.2rem;
    color: #555;
    margin-bottom: 2rem;
    animation: scaleUp 1s ease-out 0.6s;
    opacity: 0;
    animation-fill-mode: forwards;
}

.btn-container {
    display: flex;
    justify-content: center;
    gap: 1rem;
    animation: scaleUp 1s ease-out 0.8s;
    opacity: 0;
    animation-fill-mode: forwards;
}

.btn-custom {
    padding: 0.75rem 2rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: all 0.3s ease;
}

.btn-recruiter {
    background-color: #3498db;
    border-color: #3498db;
    color: white;
}

.btn-recruiter:hover {
    background-color: #2980b9;
    border-color: #2980b9;
    animation: pulse 0.5s ease-in-out infinite;
}

.btn-seeker {
    background-color: #2ecc71;
    border-color: #2ecc71;
    color: white;
}

.btn-seeker:hover {
    background-color: #27ae60;
    border-color: #27ae60;
    animation: pulse 0.5s ease-in-out infinite;
}

.footer {
    background-color: rgba(52, 73, 94, 0.9);
    color: #ecf0f1;
    padding: 1rem 0;
    margin-top: 3rem;
}

    </style>
</head>
<body class="d-flex flex-column min-vh-100">


    <main class="container mt-4">
   
